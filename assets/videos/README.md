# Erdoor resource videos

Add the approved website-ready MP4 files here using these names:

- `installation.mp4`
- `fire-resistance-test.mp4`

The corresponding Resource pages detect the files automatically and reveal the native video player when each file is available.
